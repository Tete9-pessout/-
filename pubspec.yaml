name: your_project
description: A new Flutter project

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^5.2.1  # تحديث إلى أحدث إصدار
  video_player: ^2.8.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/correct.mp3
    - assets/videos/  # تحديد ملف الفيديو صراحة
